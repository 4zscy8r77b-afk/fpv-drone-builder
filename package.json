{
  "name": "fpv-drone-builder-premium",
  "version": "2.1.1",
  "private": true,
  "description": "Premium FPV drone builder with a unified component catalog, compatibility engine and interactive 3D preview.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/*.test.js",
    "verify:layout": "node scripts/verify-layout.js",
    "check": "npm run verify:layout && npm test && node --check server.js && node --check server/index.js && node --check server/compatibility.js && node --check server/autobuild.js && node --check server/store.js && node --check public/assets/js/app.js && node --check public/assets/js/three-preview.js && node --check public/service-worker.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "three": "^0.176.0",
    "zod": "^3.25.0"
  }
}
