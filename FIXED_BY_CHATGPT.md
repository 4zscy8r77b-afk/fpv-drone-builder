# Fixed version

Replaced `index.html`.

Fixed:
- JS errors that broke Auto build and component rendering.
- Hero is first screen: "Fly everywhere. Fly anytime."
- Choose mission section.
- Choosing a mission scrolls to Builder.
- Builder filters compatible parts.
- Auto build selects a complete compatible build.
- Auto build scrolls to Drone preview.
- Drone preview changes size/profile by build type.
- Uses local build images from /assets/builds/*.jpg.
- Removed undefined variables like `issuesEl` and unsafe `budget.value` usage.

Upload the full project or replace only `index.html` in GitHub, then deploy latest commit on Render.
